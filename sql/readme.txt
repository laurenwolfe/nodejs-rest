Adding a new query:

- Add sql statement into appropriate subdirectory
- In sql.js, add a var name and link the file path
- in index.js, add the query function, which outputs json
- in routes/index.js, add url string